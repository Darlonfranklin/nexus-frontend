import { Button as ButtonUI, Typography } from "@mui/material";
import styled from "styled-components";

export const Button = styled(ButtonUI)`
    margin-left: 5px !important;
`;

export const RowsLength = styled(Typography)`
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    margin-top: 30px !important;
    color: gray;
`;
